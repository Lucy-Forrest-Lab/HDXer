name: test
channels:
  - conda-forge
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - numpy
  - mdtraj

    # Pip-only installs
  #- pip:
  #  - codecov

